#!/usr/bin/env sh
set -eu

# prime-agent-bootstrap-kernel.sh — bootstrap prime-agent's IPython kernel at
# startup (the embedded IPython tool needs a live kernel).
#
# PLACEHOLDER (GSA-TTS/agentic-coding-patterns#369): kit-shape skeleton only.
# The real kernel-bootstrap logic is fleshed out in
# GSA-TTS/agentic-coding-patterns#373. Do not add real logic here; it needs live
# testing in that issue.

echo "prime-agent: bootstrap-kernel placeholder (#369) — real bootstrap lands in #373"
