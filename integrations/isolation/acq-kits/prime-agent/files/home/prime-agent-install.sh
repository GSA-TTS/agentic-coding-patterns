#!/usr/bin/env sh
set -eu

# prime-agent-install.sh — install the prime-agent CLI + runtime (create-time).
#
# PLACEHOLDER (GSA-TTS/agentic-coding-patterns#369): kit-shape skeleton only.
# The real install logic — fetching the prime-agent bundle, wiring the CA bundle,
# and installing the Python/IPython runtime — is fleshed out in
# GSA-TTS/agentic-coding-patterns#370/#371/#372/#373. Do not add real install
# steps here; they need live testing in those issues.

echo "prime-agent: install placeholder (#369) — real install lands in #370/#371"
