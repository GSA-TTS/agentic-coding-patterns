#!/usr/bin/env sh
set -eu

# prime-agent-install-models-json.sh — drop the USAi model catalog into
# prime-agent's config location at startup.
#
# PLACEHOLDER (GSA-TTS/agentic-coding-patterns#369): kit-shape skeleton only.
# The real logic — copying the GENERATED models.json (#362 emitter) into the
# path prime-agent reads, without clobbering unrelated config — is fleshed out in
# GSA-TTS/agentic-coding-patterns#372. Do not add real logic here.

echo "prime-agent: install-models-json placeholder (#369) — real step lands in #372"
