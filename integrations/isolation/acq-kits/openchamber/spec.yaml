# spec.yaml — openchamber (a neutral acq mixin kit, hybrid/v1)
#
# Runs OpenChamber, a web UI for OpenCode, inside the sandbox and ships an
# `opencode` wrapper that owns a single, shared OpenCode server. Applying this
# kit gives you a browser-based OpenCode plus an optional terminal TUI that both
# drive the SAME live session. It is OPT-IN: it is not one of the default GSA
# kits. Apply it with an extra kit ref (see README.md "Usage").
#
# NEUTRAL (backend-agnostic) form. Converted from the former sbx kit
# (`sbx-kits/openchamber/`, schemaVersion "2") in the acq-kits migration
# follow-up (#223); see
# integrations/isolation/docs/decisions/0001-neutral-hybrid-v1-acq-kits.md and
# this kit's docs/decisions/. The inline startup shell of the old sbx kit was
# extracted into standalone scripts (files/home/...), matching how
# agentic-coding-playbook handles its startup script.
#
# DESIGN (redesigned — see docs/decisions/startup-owns-shared-server.md):
#   - The STARTUP script (openchamber-start.sh) owns the shared server: on every
#     sandbox start it installs OpenChamber (first boot only) and supervises BOTH
#     a single shared `opencode serve` on 0.0.0.0:4096 AND OpenChamber (skip-start,
#     bound to 0.0.0.0:3000, attaching to :4096). Because a `startup` command
#     fires on EVERY start — including a detached `acq create` with nobody
#     attached, held open by the sandbox's tini keepalive (PID 1) — the server
#     and UI come up on their own. A single `acq create` is enough; no `acq exec`
#     and no `acq run` are needed to bring the server up. There is deliberately
#     NO install phase — see the install note below.
#   - The sandbox entrypoint runs the bare command `opencode`. This kit drops an
#     `opencode` WRAPPER at ~/.local/bin/opencode, which is first on the image
#     PATH (ahead of the npm-global bin where the real opencode lives), so the
#     wrapper is what the entrypoint runs on the interactive `acq run` path. With
#     arguments it passes through to the real opencode; with NO arguments it
#     prints host-connect instructions and offers to attach a TUI now (only if
#     interactive). If you attach, the TUI runs as a CHILD; quitting it returns to
#     the wrapper. In every no-arg case the wrapper does NOT return on `acq run` —
#     it holds PID 1 in the FOREGROUND so THAT session's sandbox stays open (see
#     the "entrypoint must not exit" note below). The wrapper NO LONGER starts or
#     owns the server; the startup script does.
#
# WHY THE WRAPPER (entrypoint) MUST NOT RETURN on `acq run`: a Docker sandbox is
# "running" only while its entrypoint / PID 1 is alive. On the interactive
# `acq run` path the wrapper IS the entrypoint (via PATH-shadowing), so a no-arg
# path that returns — or that execs a TUI you later quit — lets PID 1 exit and
# the sandbox transitions running -> stopped. The wrapper therefore ends the
# no-arg path by BLOCKING forever (hold_pid1); an attached TUI runs as a CHILD
# and quitting it falls through to that hold. (On the detached `acq create` path
# PID 1 is instead a tini keepalive shim and the wrapper is not the entrypoint,
# so nothing here is needed to keep a detached sandbox alive.) See the ADRs.
#
# Why the startup script runs in the BACKGROUND: the OpenChamber supervisor loop
# never exits (it respawns forever), so the startup command must not block
# sandbox start. Backgrounding is expressed via the NEUTRAL `background: true`
# flag on the startup commands[] entry (schema hybrid/v1, added in #276): acq's
# translator/adapter maps it to each backend's native detached hook — sbx and
# msb both consume it — so no per-backend `backend_extras` is needed anymore.
# The startup command ALSO backgrounds the script itself with a trailing `&`
# (`sh …openchamber-start.sh &`); this is kept deliberately as belt-and-suspenders
# (see the trailing-`&` decision note on the startup command below).
#
# Why install at STARTUP, not an install phase: @openchamber/web depends on
# better-sqlite3, a NATIVE module whose npm install runs
# `prebuild-install || node-gyp rebuild` — it downloads a prebuilt binary and
# only compiles from source if that fails. The opencode base image has no C
# toolchain, so a failed download means the compile fails too; as a CREATE-TIME
# install hook that non-zero exit crashes sandbox create. Installing at startup
# keeps it OFF the create path (a failure degrades to "UI unavailable", never a
# dead sandbox) and is where the egress proxy + its CA are fully usable.
#
# AUTH: the shared `opencode serve` is UNSECURED (no OPENCODE_SERVER_PASSWORD).
# The sandbox — an ephemeral container with proxied, allow-listed egress and no
# host FS — is the security boundary, and the published ports are host loopback
# only. OpenChamber (skip-start) attaches to the unsecured server, and a host
# TUI can attach directly without a credential. See the README "Security note".
#
# PORT FORWARDING: OpenChamber binds container port 3000 and the shared server
# binds container port 4096; BOTH are published to the host. This is declared via
# the NEUTRAL top-level `publishedPorts:` list (schema hybrid/v1, added in #276),
# using the `guest:` port key (host omitted → the backend defaults host to the
# guest value). acq's translator/adapter maps publishedPorts to each backend's
# native port-publish primitive — sbx and msb both consume it — so the kit works
# on both backends with no per-backend `backend_extras`. See the README
# backend-parity note.
schemaVersion: "hybrid/v1"
kind: mixin
name: openchamber
displayName: OpenChamber (web UI for OpenCode)
description: >
  Runs OpenChamber, a browser UI for OpenCode, inside the sandbox and ships an
  `opencode` wrapper that owns a single shared OpenCode server. Installs the
  OpenChamber CLI on first boot and supervises OpenChamber on 0.0.0.0:3000; the
  wrapper starts a shared `opencode serve` on 0.0.0.0:4096 on demand so the
  browser and an optional terminal TUI share one live session. Both ports are
  published to the host. Opt-in mixin; composes with the USAi provider and the
  other GSA kits.

caps:
  network:
    allow:
      # 1. The install script is fetched from GitHub raw (at startup, first boot),
      #    from a PINNED tag — not `main` (see the pins in the startup command).
      - raw.githubusercontent.com
      # 2. npm downloads @openchamber/web and its deps from the npm registry.
      #    Both metadata and tarballs are served from registry.npmjs.org (npm's
      #    dist.tarball URLs point at this host), so no wildcard subdomain is
      #    needed. NOTE: do NOT use a leading-`*` host here (e.g. "*.npmjs.org") —
      #    acq's neutral→sbx translator emits allow entries UNQUOTED, and a bare
      #    `- *.npmjs.org` is parsed by YAML as an alias, breaking the translated
      #    spec ("did not find expected alphabetic or numeric character").
      - registry.npmjs.org
      # 3. @openchamber/web depends on better-sqlite3, a native module whose
      #    install downloads a PREBUILT binary from GitHub release assets (and only
      #    compiles from source if that download is blocked — which fails here, no
      #    toolchain). Allow the release-asset hosts so it always uses the prebuilt.
      - github.com
      - objects.githubusercontent.com
      - release-assets.githubusercontent.com

files:
  # The `opencode` wrapper. Dropped at ~/.local/bin/opencode, which is FIRST on
  # the base-image PATH, so it shadows the real opencode and is what the sandbox
  # entrypoint (bare `opencode`) runs. See the header DESIGN note.
  - path: /home/agent/.local/bin/opencode
    mode: "0755"
    source: files/home/.local/bin/opencode
    description: opencode wrapper — offers a TUI, holds PID 1 on `acq run`, passes other args through
  - path: /home/agent/openchamber-start.sh
    mode: "0755"
    source: files/home/openchamber-start.sh
    description: Install (first boot) and supervise the shared opencode serve + OpenChamber

commands:
  # Run the extracted startup script as the agent user at every start. The
  # never-exiting supervisor loops (shared server + OpenChamber) must not block
  # sandbox start, so this command is marked neutral `background: true` — acq's
  # translator/adapter maps that to each backend's native detached hook (sbx and
  # msb both consume it). The OpenChamber release pins are exported here (the
  # script also carries matching fallback defaults); bump BOTH together with the
  # script's defaults to adopt a newer release. See docs/decisions/.
  #
  # TRAILING `&` DECISION (kept): the `sh -c` body ALSO ends the script launch
  # with a trailing `&`. We KEEP it deliberately as belt-and-suspenders. It is NOT
  # a double-background problem: `background: true` tells the backend to run this
  # WHOLE command detached (it does not await it); the inner `&` returns the
  # `sh -c` immediately having spawned the script. Even where a backend awaits the
  # command's outer process, that process is a short-lived `sh -c` that exits as
  # soon as the `&` spawns the script, so start never blocks. The two mechanisms
  # compose safely (they don't fork twice into an orphaned duplicate), and this is
  # the exact shape that worked on sbx before the migration — so we preserve it
  # rather than risk a start-blocking regression by removing it.
  - phase: startup
    user: "1000"
    background: true
    description: Install (first boot) and supervise the shared server + OpenChamber (background)
    command:
      - sh
      - -c
      - |
        OPENCHAMBER_REF="v1.9.10" \
        OPENCHAMBER_INSTALL_SHA256="aa268c96ddc6d7d53fc54d2e5c2312e689493ecef6ba4f69730a93d50cf33287" \
          sh /home/agent/openchamber-start.sh &

# Neutral published ports (schema hybrid/v1, added in #276). Publish the two
# container/guest ports the kit binds: 3000 (OpenChamber UI) and 4096 (the shared
# `opencode serve`). Uses the NEUTRAL shape — each entry declares `guest:` (NOT
# the old sbx `container:` key), `protocol:`, and a `name:`. Host is omitted, so
# each backend defaults the host port to the guest value (or an ephemeral host
# loopback port, backend's choice — matching the prior sbx behavior). acq's
# translator/adapter maps these to each backend's native port-publish primitive,
# so sbx and msb both publish the ports at create time with no manual step.
publishedPorts:
  - guest: 3000
    protocol: tcp
    name: openchamber
  - guest: 4096
    protocol: tcp
    name: opencode-server

agentContext: |
  ## OpenChamber (web UI for OpenCode)

  This sandbox runs **OpenChamber**, a browser UI for OpenCode, in the
  background on container port 3000, plus a single **shared** `opencode serve`
  on `0.0.0.0:4096`. **Both are started and supervised by the kit's startup
  script on every sandbox start** — including a detached `acq create` with
  nobody attached — so they come up on their own. OpenChamber and any attached
  TUI drive that one server, so they share one live session.

  **No terminal needed.** You do NOT have to run `opencode` or keep a terminal
  in the foreground. A single `acq create --name <sandbox> opencode <path>`
  brings up the server and the UI; then find the host ports with
  `acq ports <sandbox>` and open the browser. The `opencode` **wrapper** (at
  `~/.local/bin/opencode`, first on PATH) is only needed if you want a terminal
  TUI: running it with **no arguments** prints host-connect instructions and, on
  an interactive terminal, offers to attach a TUI (it then holds the session
  open so an `acq run` sandbox stays running). Running `opencode <args…>` (e.g.
  `opencode run …`, `opencode auth login`) passes straight through to the real
  opencode. Note: the connect instructions only print when you run the wrapper
  (the `acq run` path) — after a detached `acq create`, use `acq ports`.

  **Reaching it from the host.** The kit publishes container ports 3000
  (OpenChamber) and 4096 (the raw server); acq's active backend maps each to an
  ephemeral host loopback port, per sandbox (so multiple sandboxes don't
  collide). Find them and open/attach:

  ```
  acq ports <sandbox>        # (sbx ports <sandbox>) — host ports for 3000 and 4096
  # OpenChamber:   open http://127.0.0.1:<host-port-for-3000>
  # host TUI:      opencode attach http://127.0.0.1:<host-port-for-4096>
  ```

  **Session sharing.** Because the shared server backs both the browser and any
  attached TUI, they share one live, in-flight session by default. To attach a
  TUI from the host to the same session:

  ```
  acq exec <sandbox> -- opencode attach http://127.0.0.1:4096
  ```

  The shared server is **unsecured** (no password): the sandbox is the security
  boundary and the published ports are host loopback only.

  Logs: `/tmp/openchamber.log` (OpenChamber) and
  `~/.local/state/openchamber/opencode-serve.log` (the shared server). Both run
  under respawn loops (`supervisor:opencode-serve`, `supervisor:openchamber`), so
  if either exits — for example after an interactive self-update, which needs a
  restart to take effect — it comes back a few seconds later on its own; those
  logs show `[supervisor] ... restarting` lines when it happens.

# No backend_extras / backend_shortcuts: ports and background are now declared
# with the NEUTRAL publishedPorts + background vocabulary above (schema hybrid/v1,
# #276), which acq's translator/adapter feeds to both the sbx and msb backends.
# Every backend runs the same install+supervise script and the same wrapper.
#
# CAVEAT (acq translation): acq's neutral→sbx translator (kit_translate_to_sbx)
# reads the neutral `publishedPorts` FIRST (only falling back to a legacy
# backend_extras.sbx.publishedPorts with a deprecation warning — which this kit no
# longer has). So a current `acq` publishes container ports 3000/4096 to the host
# at create time with no manual step.
